import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '01_manage_class',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
